# Copy this file to .env.local for local development (vercel dev),
# and add the same variable in Vercel -> Project Settings -> Environment
# Variables for production. Never commit the real key.

# Anthropic API key used by /api/ask.js (server-side only — never exposed
# to the browser). Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=
